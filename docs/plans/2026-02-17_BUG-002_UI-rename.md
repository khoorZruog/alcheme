# BUG-002 修正 + UI用語リネーム ("My Cosme" / "Next Cosme")

| | |
|---|---|
| **Date** | 2026-02-17 |
| **Status** | Completed |
| **Scope** | BUG-002 修正、UI用語変更 |

---

## Context

1. **BUG-002**: 買い足し（suggestions）の手動追加で「追加に失敗しました」エラー
2. **UI用語変更**: 「在庫/Vanity/コスメ」→ **My Cosme**、「買い足し」→ **Next Cosme**
   - DB値・変数名はそのまま、ユーザーに見えるUIテキストのみ変更

---

## Step 1: BUG-002 修正

### 原因

`app/api/suggestions/route.ts` の POST ハンドラ（L90-101）で、リクエストボディに含まれないフィールド（`item_type`, `product_url` 等）が `undefined` のまま `docRef.set()` に渡される。Firestore は `undefined` を拒否するため 500 エラー。

AI経由の候補追加は全フィールドが埋まるため正常動作するが、手動追加フォームは最小限のフィールドしか送らないため失敗する。

### 修正箇所

**`app/api/suggestions/route.ts`** (L90-101)

`docRef.set()` に渡すオブジェクトから `undefined` 値を除外:

```typescript
const doc: Record<string, unknown> = {
  brand,
  product_name,
  reason: reason || '',
  recommendation_count: 1,
  history: [historyEntry],
  status: '候補',
  source: source || 'manual',
  created_at: now,
  updated_at: now,
};
// Optional fields — only include if defined
if (color_code) doc.color_code = color_code;
if (color_name) doc.color_name = color_name;
if (category) doc.category = category;
if (item_type) doc.item_type = item_type;
if (price_range) doc.price_range = price_range;
if (product_url) doc.product_url = product_url;
if (image_url) doc.image_url = image_url;

await docRef.set(doc);
```

---

## Step 2: UI用語リネーム

### 変更対象（ユーザーに見えるUIテキストのみ）

| ファイル | 行 | 旧 | 新 |
|---------|---|---|---|
| `components/bottom-nav.tsx` | 9 | `"Vanity"` | `"My Cosme"` |
| `components/bottom-nav.tsx` | 11 | `"買い足し"` | `"Next Cosme"` |
| `components/side-menu.tsx` | 21 | `"コスメ"` | `"My Cosme"` |
| `components/side-menu.tsx` | 23 | `"買い足し"` | `"Next Cosme"` |
| `app/(main)/suggestions/page.tsx` | 225 | `title="買い足し"` | `title="Next Cosme"` |
| `app/(main)/suggestions/page.tsx` | 357 | `"買い足し候補がありません"` | `"Next Cosme がありません"` |
| `app/(main)/suggestions/add/page.tsx` | 51 | `"買い足し候補に追加しました！"` | `"Next Cosme に追加しました！"` |
| `app/(main)/suggestions/add/page.tsx` | 61 | `"候補を追加"` | `"Next Cosme を追加"` |
| `app/api/suggestions/route.ts` | 1 | コメント `買い足し候補一覧` | `Next Cosme 一覧` |
| `types/suggestion.ts` | 13 | コメント `買い足し候補アイテム` | `Next Cosme アイテム` |

### 変更しないもの

- **DB値**: `suggested_items` コレクション名、ステータス値（`候補`, `購入済み`, `見送り`）
- **変数名**: `suggestions`, `SuggestedItem`, `useSuggestions` 等
- **APIルートパス**: `/api/suggestions`
- **エージェントプロンプト**: `agent/alcheme/prompts/*.py` 内の「手持ち」「在庫」「買い足し」（AI応答の自然さに影響するため別タスクで検討）
- **ドキュメント**: `docs/` 内の用語（別タスクでまとめて更新）

---

## Step 3: 検証

1. `npx tsc --noEmit` — 型エラーなし
2. `npx vitest run` — 全テストパス（25ファイル / 127テスト）
3. 手動テスト（任意）: `/suggestions/add` で手動追加が成功すること

---

## 影響範囲

| 変更 | ファイル数 | リスク |
|------|----------|--------|
| BUG-002 修正 | 1 (route.ts) | 低（undefined除外のみ） |
| UI用語リネーム | 6 (tsx/ts) | 低（表示テキストのみ） |
