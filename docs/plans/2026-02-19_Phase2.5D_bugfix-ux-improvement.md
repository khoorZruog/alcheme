# Phase 2.5D: バグ修正 + UX 改善 7 件

> **Date**: 2026-02-19
> **Status**: Complete

## Context

Phase 2.5C (Chat UI 改善) 完了後、ユーザーが `npm run dev` で手動テストし発見した 7 件の問題。バグ修正 (4件) + UX 改善 (3件) に分類。SNS 導線改善は FEAT-002 残存課題として **Phase 3 に移行**（フォロー機能は API・Hook・UI 実装済みだが発見性が低い）。

---

## Batch A: バグ修正（完了）

### A1. チャットヘッダーの "A" サークル削除
- **ファイル:** `app/(main)/chat/page.tsx`
- グラデーション "A" サークル div を削除

### A2. メイク日記フォームの戻るボタン修正
- **ファイル:** `components/page-header.tsx`, `app/(main)/beauty-log/page.tsx`
- `PageHeader` に `onBack?: () => void` prop 追加（`onBack` > `backHref` > `router.back()` 優先順位）
- `beauty-log/page.tsx` で `backHref="/beauty-log"` → `onBack={handleFormCancel}`
- Suspense 境界追加（Next.js 15 `useSearchParams` 要件）

### A3. ストーリーズトレイの遷移先修正
- **ファイル:** `components/beauty-stories-tray.tsx`, `app/(main)/beauty-log/page.tsx`
- 「+」ボタン: `/beauty-log` → `/beauty-log?new=true`
- 空の日サークル: `/beauty-log` → `/beauty-log?date=${day.date}`
- `beauty-log/page.tsx` で `useSearchParams` + `useEffect` で `date`/`new` パラメータ読み取り → 自動フォーム表示

### A4. ラベル統一: 「手持ちコスメ一致率」
- 5 ファイルの `再現度` / `一致率` → `手持ちコスメ一致率` に統一
- 未適用の 3 箇所に色分け追加（≥80% green, ≥50% amber, <50% red）

| ファイル | 変更 |
|---------|------|
| `app/(main)/recipes/[recipeId]/page.tsx` | `再現度` → `手持ちコスメ一致率` + 色分け |
| `app/(main)/recipes/page.tsx` | `再現度` → `手持ちコスメ一致率` + 色分け |
| `app/(main)/inventory/[itemId]/page.tsx` | `再現度` → `手持ちコスメ一致率` + 色分け |
| `components/recipe-card-inline.tsx` | `一致率` → `手持ちコスメ一致率` |
| `components/recipe-list-item.tsx` | `一致率` → `手持ちコスメ一致率` |

---

## Batch B: レシピ詳細リデザイン（完了）

### B1. プロのコツ — グラスカード + ナンバリング
- グラスモーフィズム + グラデーション枠 (`bg-linear-to-br from-neon-accent/30`)
- 各 tip にナンバリング付き circle

### B2. フィードバック — glass-card ラッパー
- `components/recipe-feedback.tsx` に `glass-card rounded-card p-4` ラッパー追加
- ヘッダーを `font-bold` に変更

### B3. 代用品の明確化
- `components/recipe-step-card.tsx`: `item_id` あり + `substitution_note` なし → 緑「手持ち」バッジ
- `substitution_note` あり → 塗り背景「代用テク」バッジ (`bg-alcheme-warning/15`)
- レシピ詳細にサマリー行: 「手持ち X品 / 代用 Y品 / 全Z品」
- `Badge` コンポーネント依存を削除（インラインスタイルに変更）

### B4. PC レスポンシブ 2 カラムレイアウト
- `lg:grid lg:grid-cols-[400px_1fr] lg:gap-8 lg:max-w-5xl lg:mx-auto`
- 左: タイトル + 画像 + メタ + AI思考（sticky）
- 右: サマリー + ステップ + プロのコツ + 使用履歴 + フィードバック
- 画像に `lg:max-h-[500px]` 制約追加

---

## Batch C: マイページカテゴリ改善（完了）

### C1. ツールチップ + クリックフィルタ
- **ファイル:** `components/profile/profile-category-badges.tsx`
  - `HelpCircle` に CSS ベースのホバーツールチップ（「登録されたコスメのカテゴリ分布」）
  - `<div>` → `<Link href="/inventory?category=...">` に変更
- **ファイル:** `app/(main)/inventory/_components/inventory-have-view.tsx`
  - `useSearchParams` から `category` を読み取り → `setFilter()` 呼び出し

---

## 新規テスト

| ファイル | テスト数 |
|---------|---------|
| `__tests__/unit/components/page-header.test.tsx` | 5 |
| `__tests__/unit/components/recipe-step-card.test.tsx` | 6 |

---

## 検証結果

- `npx tsc --noEmit` — エラーなし
- `npx vitest run` — 34 files / 188 tests 全パス
