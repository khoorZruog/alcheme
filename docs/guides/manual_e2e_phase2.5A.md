# Phase 2.5A ナビゲーション再構築 — 手動E2Eテスト手順書

| | |
|---|---|
| **対象** | Phase 2.5A Batch 1-4 全変更 |
| **前提** | ログイン済み、在庫・レシピ・Beauty Log が各1件以上存在 |
| **デバイス** | モバイル表示（Chrome DevTools 390x844 等） |

---

## 1. BottomNav（タブバー）

### TC-1.1: タブ構成の確認

| # | 手順 | 期待結果 |
|---|------|----------|
| 1 | アプリを開く | BottomNav に5つのアイコンが表示される |
| 2 | 左から順にアイコンを確認 | Sparkles / LayoutGrid / **Plus（中央大）** / BookOpen / User |
| 3 | 中央「+」ボタンの外観を確認 | 52x52 circle、グラデーション背景（ネオン→ピンク）、白い Plus アイコン、ナビバーから少し上に飛び出し |

### TC-1.2: タブ遷移

| # | 手順 | 期待結果 |
|---|------|----------|
| 1 | Tab 1（Sparkles）をタップ | `/chat` に遷移、アイコンがアクティブ状態（白背景 + アクセントカラー + ドット） |
| 2 | Tab 2（LayoutGrid）をタップ | `/inventory` に遷移 |
| 3 | Tab 4（BookOpen）をタップ | `/recipes` に遷移 |
| 4 | Tab 5（User）をタップ | `/mypage` に遷移 |
| 5 | 各タブのアクティブ状態を確認 | 現在のタブのみアクティブ（白背景 + ドット） |

---

## 2. Quick Action Sheet（中央「+」ボタン）

### TC-2.1: シートの開閉

| # | 手順 | 期待結果 |
|---|------|----------|
| 1 | 中央「+」ボタンをタップ | Quick Action Sheet がボトムから表示される |
| 2 | シートのタイトルを確認 | 「今日を記録する」が表示 |
| 3 | シート外をタップまたはドラッグダウン | シートが閉じる |

### TC-2.2: アクション遷移

| # | 手順 | 期待結果 |
|---|------|----------|
| 1 | 「今日のメイクを記録」をタップ | `/beauty-log?action=new` に遷移 |
| 2 | 戻って「+」→「AIに相談」をタップ | `/chat` に遷移 |
| 3 | 戻って「+」→「コスメ追加」をタップ | AddMethodSheet（スキャン鑑定/楽天検索/Web検索/手動）が開く |
| 4 | 戻って「+」→「レシピ作成」をタップ | `/recipes/create` に遷移 |
| 5 | 戻って「+」→「欲しい追加」をタップ | `/suggestions/add` に遷移 |

---

## 3. My Cosme 統合（セグメントタブ）

### TC-3.1: セグメント表示

| # | 手順 | 期待結果 |
|---|------|----------|
| 1 | Tab 2（My Cosme）をタップ | `/inventory` に遷移。ヘッダーに「My Cosme」、その下にセグメントタブ「持ってる」「欲しい」 |
| 2 | デフォルト状態を確認 | 「持ってる」が選択状態（太字 + 白背景 + シャドウ） |
| 3 | 在庫カウントの表示を確認 | セグメントタブの横に件数が表示される（存在する場合） |

### TC-3.2: セグメント切替

| # | 手順 | 期待結果 |
|---|------|----------|
| 1 | 「欲しい」タブをタップ | 欲しいリスト（Next Cosme）が表示される。URL が `/inventory?tab=want` に変更 |
| 2 | フィルタ（候補/購入済/見送り）が表示されることを確認 | ステータスフィルタが表示される |
| 3 | 「持ってる」タブに戻る | 在庫リストが表示される。URL が `/inventory?tab=have` に変更 |
| 4 | カテゴリフィルタが表示されることを確認 | カテゴリフィルタピルが表示される |

### TC-3.3: 各セグメントの操作

| # | 手順 | 期待結果 |
|---|------|----------|
| 1 | 「持ってる」でグリッド/リスト切替 | 表示が切り替わる |
| 2 | 「持ってる」でカテゴリフィルタを選択 | フィルタされた結果が表示される |
| 3 | 「欲しい」でステータスフィルタを選択 | フィルタされた結果が表示される |
| 4 | 「持ってる」の+ボタンをタップ | AddMethodSheet が開く |
| 5 | 「欲しい」の+ボタンをタップ | `/suggestions/add` に遷移 |

### TC-3.4: リダイレクト確認

| # | 手順 | 期待結果 |
|---|------|----------|
| 1 | URL バーに `/suggestions` を直接入力 | `/inventory?tab=want` にリダイレクトされる |
| 2 | リダイレクト中にローディング表示が出ることを確認 | グラデーション pulse + 「リダイレクト中...」 |

---

## 4. マイページ

### TC-4.1: マイページ表示

| # | 手順 | 期待結果 |
|---|------|----------|
| 1 | Tab 5（User）をタップ | `/mypage` に遷移 |
| 2 | ヘッダーを確認 | 「マイページ」タイトル + 右上に歯車（設定）アイコン |
| 3 | プロフィール Hero を確認 | アバター、表示名、統計（投稿/レシピ/フォロワー/フォロー） |
| 4 | Beauty Tags を確認 | 性別、年齢層、肌質、パーソナルカラー等のタグ |
| 5 | Best Cosme セクションを確認 | ベストコスメの横スクロール（設定済みの場合） |

### TC-4.2: Beauty Log プレビュー

| # | 手順 | 期待結果 |
|---|------|----------|
| 1 | マイページをスクロールして Beauty Log セクションを探す | 「メイク日記」セクションが表示 |
| 2 | ミニカレンダーを確認 | 当月のカレンダーが表示、記録日にドットが表示される |
| 3 | 直近のログエントリを確認 | 最大3件のログが日付・サムネイル・ムード・評価付きで表示 |
| 4 | 「もっと見る」をタップ | `/beauty-log` に遷移 |

### TC-4.3: コンテンツタブ

| # | 手順 | 期待結果 |
|---|------|----------|
| 1 | 「投稿」「レシピ」「日記」タブを切替 | 各タブのコンテンツグリッドが切り替わる |

### TC-4.4: 設定遷移

| # | 手順 | 期待結果 |
|---|------|----------|
| 1 | 右上の歯車アイコンをタップ | `/settings` に遷移 |
| 2 | 設定ページの戻るボタンをタップ | `/mypage` に戻る |

### TC-4.5: プロフィールリダイレクト

| # | 手順 | 期待結果 |
|---|------|----------|
| 1 | URL バーに `/profile` を直接入力 | `/mypage` にリダイレクトされる |

---

## 5. Beauty Log ページ

### TC-5.1: ヘッダー変更確認

| # | 手順 | 期待結果 |
|---|------|----------|
| 1 | `/beauty-log` に遷移 | PageHeader が表示される（タブヘッダーではなく戻るボタン付き） |
| 2 | 右側にカレンダー/タイムライン切替 + 記録ボタンが表示されることを確認 | ビュー切替トグル + 「記録」ボタン |
| 3 | 戻るボタンをタップ | `/mypage` に遷移 |

---

## 6. Recipe タブ

### TC-6.1: デフォルトビュー

| # | 手順 | 期待結果 |
|---|------|----------|
| 1 | Tab 4（Recipe）をタップ | `/recipes` に遷移 |
| 2 | デフォルト表示モードを確認 | **グリッド表示**がデフォルト（2カラム） |
| 3 | ヒーロー CTA カードが表示されることを確認 | 「AIにメイクレシピを相談」カードがグリッドの上に表示（レシピが1件以上ある場合） |
| 4 | ヒーロー CTA をタップ | `/chat` に遷移 |

### TC-6.2: お気に入りクイックフィルタ

| # | 手順 | 期待結果 |
|---|------|----------|
| 1 | Recipe タブでヘッダー下のクイックフィルタバーを確認 | 「お気に入り」ボタン + シーンフィルタ（最大3つ）が表示 |
| 2 | 「お気に入り」をタップ | ボタンがアクティブ状態に変わり、お気に入りレシピのみ表示 |
| 3 | 再度「お気に入り」をタップ | フィルタ解除、全レシピ表示 |
| 4 | シーンフィルタをタップ | 該当シーンのレシピのみ表示 |

### TC-6.3: グリッド/リスト切替

| # | 手順 | 期待結果 |
|---|------|----------|
| 1 | ヘッダー右のリスト切替ボタンをタップ | リスト表示に切り替わる |
| 2 | 再度タップ | グリッド表示に戻る |

---

## 7. サイドメニュー

### TC-7.1: メニュー項目

| # | 手順 | 期待結果 |
|---|------|----------|
| 1 | サイドメニューを開く（ハンバーガーメニュー or スワイプ） | メニューが表示される |
| 2 | MAIN メニュー項目を確認 | AI美容部員 / My Cosme / レシピ / メイク日記 |
| 3 | SUB メニュー項目を確認 | マイページ / フィード / 設定 |
| 4 | 各メニュー項目をタップ | 正しいページに遷移する |

---

## 8. エッジケース

### TC-8.1: データなし状態

| # | 手順 | 期待結果 |
|---|------|----------|
| 1 | レシピが0件の状態で Recipe タブを開く | EmptyState が表示、ヒーロー CTA は非表示 |
| 2 | Beauty Log が0件の状態でマイページを開く | Beauty Log プレビューに「まだログがありません」表示 |
| 3 | 在庫が0件の状態で「持ってる」を開く | EmptyState が表示 |
| 4 | 欲しいリストが0件の状態で「欲しい」を開く | EmptyState が表示 |

### TC-8.2: ブラウザ戻る/進む

| # | 手順 | 期待結果 |
|---|------|----------|
| 1 | `/inventory` → 「欲しい」タブ → ブラウザ戻る | 「持ってる」タブまたは前ページに戻る（`router.replace` のためヒストリ汚染なし） |
| 2 | `/mypage` → `/settings` → ブラウザ戻る | `/mypage` に戻る |

---

## チェックリスト（総合）

- [ ] BottomNav 5タブ表示・遷移（TC-1.1, TC-1.2）
- [ ] 中央「+」Quick Action Sheet 全5アクション（TC-2.1, TC-2.2）
- [ ] My Cosme セグメント切替（TC-3.1, TC-3.2, TC-3.3）
- [ ] `/suggestions` リダイレクト（TC-3.4）
- [ ] マイページ全セクション表示（TC-4.1, TC-4.2, TC-4.3）
- [ ] マイページ→設定→戻る（TC-4.4）
- [ ] `/profile` リダイレクト（TC-4.5）
- [ ] Beauty Log ヘッダー・戻り先（TC-5.1）
- [ ] Recipe グリッドデフォルト + ヒーロー CTA（TC-6.1）
- [ ] Recipe お気に入りクイックフィルタ（TC-6.2）
- [ ] サイドメニュー項目（TC-7.1）
- [ ] エッジケース（TC-8.1, TC-8.2）
